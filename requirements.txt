# NBDScanner - Consolidated Non-B DNA Motif Detection System
# Core dependencies for the modular architecture

# Web Framework
streamlit>=1.28.0

# Scientific Computing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Visualization  
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.17.0

# Bioinformatics
biopython>=1.79

# Jupyter Widgets for Interactive Notebooks
ipywidgets>=8.0.0

# Data Export
openpyxl>=3.0.0
xlsxwriter>=3.0.0

# Networking
requests>=2.28.0

# System Monitoring
psutil>=5.8.0

# Optional: Performance Acceleration (install separately for production use)
# hyperscan>=0.7.0  # High-performance pattern matching (optional acceleration)
# pyfastx>=0.8.0    # Fast FASTA/FASTQ parsing (optional acceleration)
# numba>=0.56.0   # JIT compilation for 2-5x speedup (optional; causes _frozen_importlib._DeadlockError on Streamlit Cloud)

# Genome-scale execution (notebook GENOME mode)
pyarrow>=10.0.0   # Parquet I/O for per-chromosome disk streaming (genome mode)

# === Streamlit Cloud Optimizations (NEW) ===
pyahocorasick>=2.0.0  # Aho-Corasick multi-pattern matching (50-200x speedup)
intervaltree>=3.1.0   # Efficient interval overlap detection

# Optional: Development tools
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0